{
  "name": "com.imported.neptune_flight",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "ui5 serve --port 8080 --open index.html",
    "build": "ui5 build --clean-dest",
    "ts-typecheck": "tsc --noEmit"
  },
  "ui5": {
    "dependencies": [
      "ui5-tooling-transpile"
    ]
  },
  "devDependencies": {
    "@openui5/types": "^1.136.0",
    "@ui5/cli": "^4.0.0",
    "typescript": "~5.8.3",
    "ui5-tooling-transpile": "^3.12.0"
  }
}
